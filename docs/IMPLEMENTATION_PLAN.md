# IMPLEMENTATION_PLAN — 実装計画と現状

仕様書 `SHIKA_GACHA_SPEC_v1.1`（アクセス解析削除版）を最上位仕様として実装した。

## 進捗

| Phase | 内容 | 状態 |
|---|---|---|
| 1 | データスキーマ / state / storage / ホーム / コレクション / カード詳細 / 1回・10連 / SHIKA COIN / NEW・かぶり・カテゴリ報酬 / 初回無料10連 / バックアップ | 実装済み |
| 2 | 3Dカードビューア（光沢・影・減衰・ダブルタップ復帰）/ ガチャ演出 / Web Audio / 振動 | 実装済み |
| 3 | OpenStreetMap の地図 / GPSチェックイン / 訪問報酬 / 周遊導線 / Google Maps遷移 | 実装済み |
| 4 | PWA / オフライン / 更新通知 / 段階キャッシュ | 実装済み |
| 5 | Excel→JSON / config編集ツール / プライバシー表示 / エラー処理 | 実装済み |

## ファイルの役割

| ファイル | 役割 |
|---|---|
| `js/app.js` | 起動、動作環境チェック、起動演出、ルート登録、ホーム画面 |
| `js/router.js` | ハッシュルーター（`#/card/:id` のような可変部分に対応） |
| `js/state.js` | 公開データの読み込みと正規化、参照系ヘルパー、購読 |
| `js/storage.js` | localStorage の読み書き、正規化、移行、フォールバック |
| `js/gacha.js` | 抽選、確定保存、演出、結果画面 |
| `js/rewards.js` | コイン付与ルール（デイリー・酒のアテ・かぶり・カテゴリ） |
| `js/collection.js` | コレクション一覧（カテゴリ別・気になる） |
| `js/card-detail.js` | カード詳細（グルメ／スポット／文化で出し分け） |
| `js/card-3d.js` | 3Dカードビューア |
| `js/geo.js` | 測位・距離計算・チェックイン判定（座標は保存しない） |
| `js/map.js` | まち巡り画面と OpenStreetMap タイルの簡易地図 |
| `js/offline.js` | 通信状態の表示 |
| `js/settings.js` | 設定・遊び方・プライバシー・集めた記録・リセット |
| `js/backup.js` | バックアップ書き出しと復元 |
| `js/update.js` | Service Worker 更新とデータ更新の確認 |
| `js/ui.js` | 画面部品の共通処理（仕様書の推奨構成への追加） |
| `js/dom.js` | DOM組み立ての最小ヘルパー（同上） |
| `js/card-render.js` | カードを部品から組み立てる（同上） |
| `js/gacha-anim.js` | ガチャ演出。円舞→三分→合一→抜刀（同上） |
| `js/sound.js` | Web Audio による効果音生成（同上） |

## 仕様書との差分

1. **`js/analytics.js` は作っていない。**
   仕様書§35の推奨構成にファイル名があるが、§3・§32・§40 でアクセス解析・行動追跡を禁止しているため、
   置くと誤解のもとになると判断した。解析コードは1行も入っていない。

2. **`js/ui.js` と `js/sound.js` を追加した。**
   推奨構成にない2ファイル。画面部品と効果音生成の共通処理をまとめたもので、機能追加ではない。

3. **地図に「チェックイン対象外」の場所も表示している。**
   仕様書§17ではGPS対象フラグで対象を決めるとあり、それは守っている（チェックインできるのはフラグ○のみ）。
   一方で座標を持つカード（松尾神社・高爪山・福浦港など）が地図から消えると案内として不便なため、
   灰色のピンで参考表示し、チェックイン対象と区別した。Excel で「GPS対象」に○を入れれば対象になる。

4. **カテゴリ「文化」は現在4種類。** カテゴリ5種類ごとの報酬（+2）は文化では発生しない。
   カードを1種類追加すると発生するようになる。

5. **カード032「さくら貝」のカテゴリはカード画像から「文化」と確定した。**
   Excel上は「アプリカテゴリ」が空欄だが、支給されたカード画像 `card-32.png` に「文化」と印刷されている。
   推測ではなく現物の記載に従い、`culture` として公開している。
   **ただしExcelは空欄のままなので、次に変換ツールを使うと再び非公開になる。**
   Excelの032行に「文化」を入れておくこと。

6. **カードは1枚絵をやめ、部品から組み立てる方式にした（v1.1）。**
   文言の修正のたびに画像を作り直す運用を避けるための変更。
   ジャンル共通の意匠は台紙画像にまとめ、カードごとに変わるものはデータから描く。

   | 層 | 中身 | 出どころ |
   |---|---|---|
   | ① 写真 | 産品・風景 | `assets/photos/`（Excelのセル内画像） |
   | ② 台紙 | 外枠・背景・区切り線・所在地ピル・罫線 | `assets/frames/<ジャンル>.png` |
   | ③ バッジ | 紺のピル＋アイコン＋カテゴリ名 | `assets/frames/icon-<ジャンル>.png` ＋ 描画 |
   | ④⑤⑥ | 番号・名前・説明 | `cards.json` |
   | ⑦ ボタン | 白い角丸ボタン | `cards.json` の cardButton / `config.json` の cardButtons |
   | ⑧ ロゴ | SHIKA COLLECTION | `assets/frames/logo.png` |

   台紙は現行カードから作った。写真部分・名前・説明の帯・ボタン・ロゴを消し、
   ジャンル共通の意匠だけを残してある。
   元になったカード: グルメ=card-01 / スポット=card-41 / 文化=card-32。
   実測した設計座標（1080×1350）は `css/card-art.css` の冒頭に記録してある。

   組み立てた結果を元カードと画素で突き合わせた実測値（card-01）:
   ロゴ x428–648 y1122–1258（元 x428–651 y1124–1260）、
   ボタン x96–983 y1048–1109（元 x96–984 y1048–1110）。

7. **カードの文字はシステムフォントで描く。**
   元のカードは専用フォントで焼かれていたが、Webフォントは使わない方針（§40）のため、
   端末標準のゴシックで表示する。太さ・字幅がわずかに異なる。

8. **カードに載る説明文が長いカードが10件ある。**
   元のカード画像には、Excelの説明文より短く書き直された文が焼き込まれていた。
   その短い文は画像の中にしか無く、取り出せない。
   現在は `description` を3行で切って表示している。Excelに「カード説明文」列を作って
   短い文を入れると、そちらが優先される。
   該当: 029 / 032 / 036 / 037 / 038 / 043 / 044 / 045 / 051 / 052。

9. **写真はExcelのセル内画像から取り出した。**
   「詳細写真１」列に貼られた画像がカードの元写真だった。52枚をカードIDに紐づけて抽出。
   051（西海祭り）だけ写真が無く、元のカードでも「写真準備中」になっていた。
   変換ツールにも同じ抽出機能を入れてあるので、Excelを直せば写真も更新できる。

   参考: 一度カード画像をWebP(q0.9)へ変換したが、画質優先の指示で取りやめた。
   原寸1080pxで PSNR 32.5〜33.9dB、品質を上げても色差の間引き(4:2:0)で頭打ちになる。

10. **カードの縦横比は 4:5（1080×1350）。** 当初 63:88 を想定していたが、実物に合わせて変更した。

11. **カード画像側のカテゴリとExcelのカテゴリを全51枚で照合した。**
    カード下段の帯の色（グルメ=茶 / スポット=緑 / 文化=紫）を機械的に読み取って突き合わせ、
    032以外はすべて一致することを確認済み。

## 実装判断ルール（仕様書§39の適用）

迷ったときは次の順で優先した。
個人情報を集めない → 無料で運用できる → カードデザインを壊さない → スマホで扱いやすい →
外部依存を減らす → オフラインで使える → Excelで運用変更できる → 確認を増やさない →
ガチャと観光情報を分離 → 不明な観光情報を推測しない。

具体例：
- 地図ライブラリ（Leaflet等）を使わず、タイル表示を自前で書いた（外部依存とオフライン対応のため）
- Excel読み込みも外部ライブラリを使わず、ブラウザ標準の `DecompressionStream` でzipを展開している
- 効果音は生成、アイコンはSVG、フォントは端末標準

12. **裏面・ロゴ・カテゴリアイコンは支給パーツに差し替えた（v1.2）。**
    - 裏面 `assets/cards/_back.png`（1122×1402）… ガチャ演出と3Dビューアの裏面
    - ロゴ `assets/frames/logo.png`（1536×1024 透過）… カード下部とホーム画面
    - アイコン `assets/frames/icon-<ジャンル>.png`（1254×1254 透過）… カテゴリバッジ
    - PWAアイコンも裏面デザインから作り直した（仕様§31）
    いずれも支給ファイルを無変換で同梱している。可逆再圧縮も試したが逆に大きくなったため見送った。

13. **カード下部のボタンを台紙から外し、文言をデータで決めるようにした。**
    「ほかの文言が入るかもしれない」ため。優先順は カード個別（Excel「カードボタン文言」）→
    ジャンル既定（`config.json` の `cardButtons`）→ 文言が無ければボタンを出さない。
    文字色は元カードから実測した（グルメ #4a2314 / スポット #0c3336）。

14. **ガチャ演出を作り直した（v1.3）。**
    4拍構成、全4800ms。毎フレーム transform を書き換える方式（rAF）で `js/gacha-anim.js` に分離した。

    | 拍 | 時間 | 内容 |
    |---|---|---|
    | 序 | 0–400ms | 9枚が重なった束が中央で沈む |
    | 一 円舞 | 400–1700ms | 束がほどけ、傾いた楕円（rotateX 64°）を等弧長で回る |
    | 二 三分 | 1700–2400ms | 3枚ずつ3つの束に寄る。2020/2180/2340ms の3拍でジャンル色に染まる |
    | 三 合一 | 2400–3120ms | 3つの束が渦を巻いて中心で1つになる。着弾で閃光・衝撃波・カメラの揺れ |
    | 四 抜刀 | 3120–3800ms | 1枚が抜けて540°翻り表になる。面が替わるのは3520ms |
    | 結 | 3800–4800ms | 金粉が降り、カード名が4000msにせり上がる |

    3つの束は グルメ=上 / スポット=左下 / 文化=右下 に固定。引いたカテゴリで位置は変えない
    （位置が変わると抜刀前に結果が読めてしまう）。所要時間も3カテゴリ同一で、
    変えるのは光の色と粒子の性格だけ（レアリティを作らない方針）。

    10連は 1枚目が全4800ms、2〜9枚目は円舞と三分を省いて合一の直前から2.4倍速（約860ms）、
    最後の1枚は余韻まで見せる。全体で約15秒。SKIPは1枚目の表示後（約4.8秒）から押せる。
    舞台をタップすると、その1枚の演出だけ最後まで飛ぶ。
    `prefers-reduced-motion` では動かさず最終状態だけ見せる。

15. **カード一覧の重さを直した（v1.4）。**
    53枚を並べる一覧で、原寸（写真1〜3MB・台紙330KB・アイコン1.3MB・ロゴ1.8MB）を
    そのまま横110pxに縮めて描いていたため、描画が数秒かかっていた。

    | 対策 | 効果 |
    |---|---|
    | `assets/photos/thumb/`（幅360px・JPEG）を一覧で使う | 写真 34MB → 1.2MB |
    | `assets/frames/thumb/`（台紙384px・アイコン128px・ロゴ320px） | 部品 6.8MB → 417KB |
    | 画像すべてに `loading="lazy"` | 画面外は読み込まない（53枚中30枚程度で済む） |
    | `.cell` に `content-visibility:auto` | 画面外のカードは描画しない |
    | `.card--img` の `container-type` を外す | サイズコンテナが半分に |

    縮小版が無ければ原寸に切り替わるので、`thumb/` を消しても表示は欠けない。
    変換ツールの「写真を書き出す」も、原寸と一緒に `thumb/` を作るようにした。

    注意: `contain-intrinsic-size` に1つの長さだけ書くと**幅にも効いて**3列目がはみ出す。
    `contain-intrinsic-height` で高さだけ指定すること。

16. **カード一覧で説明文が消えていたのを直した（v1.4）。**
    小さい表示のときに説明文・ボタン・カテゴリ名を隠すコンテナクエリを入れていたが、
    カードが1枚の絵として欠けて見えるため取りやめた。文字は cqw なので比率どおりに縮む。

17. **3Dビューアの×ボタンが押せなかったのを直した（v1.4）。**
    カード名の帯（`.viewer3d__caption`）が画面幅いっぱいの要素で、DOM順があとのため
    ×ボタンの上に重なってタップを奪っていた。帯を左右56pxだけ内側に寄せ、
    `pointer-events:none` を付け、ボタンに `z-index` を与えた。
    同じ理由で `.viewer3d__hint` にも `pointer-events:none` を付けた。

18. **カード詳細のカード画像も縮小版を使うようにした（v1.4）。**
    詳細画面のカードは表示幅が約150pxしかないのに原寸写真を読んでいたため、
    開いた直後に写真だけ遅れて出ていた。一覧と同じ `small` 指定にして、
    未取得カードの枠（`lockedCard`）にも縮小版が渡るようにした。
    大きく見せる3Dビューアは今までどおり原寸を読む。

19. **小さい表示のときの説明文が3行目を覗かせていたのを直した（v1.4）。**
    2行に切る指定だけでは枠の高さが3行分のままだった。高さも2行分に揃えた。

20. **ガチャ演出を画面全体にした（v1.5）。**
    `.gachastage` を `position:fixed; inset:0` にして、ヘッダーとタブの上にかぶせる。
    演出の座標は `js/gacha-anim.js` が毎フレーム舞台の実寸から計算するので、
    大きさを変えても中身は自動で追従する。抜き出すカードは `min(74vw,46vh,340px)`。
    幅と高さの両方で頭打ちにしないと、横長の画面でカードが縦にはみ出す。
    演出中は `body.is-drawing` で背後のスクロールを止め、
    途中で画面を離れたときのために `hashchange` でも必ず外す。

21. **NEW とカード名を演出面の中に移した（v1.5）。**
    以前はカード名を舞台の外（`#view` の下）に置いていたので、
    全画面にすると隠れてしまう。`.gacha__caption` にまとめて画面下寄りに固定した。
    カードの大きさが変わっても重ならない。

22. **1枚引きの結果を中央ぞろえで大きくした（v1.5）。**
    2列グリッドに1枚だけ入れていたため左上に寄っていた。`.result__solo` を使い、
    カード幅 `min(66vw,260px)` で中央に置き、説明文は `--low` で下げる。
    この1枚だけは縮小版ではなく原寸を読む（大きく出すのでぼやけさせない）。

23. **カード番号のピルをジャンルバッジとそろえた（v1.5）。**
    番号は固定幅で余白が無く、文字がピルからあふれて見えていた。
    高さ・上端をバッジと同じにし（4.815% = 6.019cqw）、幅は中身にあわせ、
    右端 x=993 を固定した。文字数が増えても外に出ない。

24. **3Dビューアのカードを大きくした（v1.5）。**
    `min(74vw,300px)` から `min(88vw,54vh,420px)` に。大きさは `--c3w` の1か所で決め、
    影の位置（CSSとJSの両方）も同じ変数を見る。

25. **カード詳細をカード中心の並びにした（v1.6）。**
    カードを画面の端まで（`.view` の左右余白を打ち消して）大きく出し、
    ジャンル・名前・ボタンはその下に置く。説明文はカードに印刷されているので、
    本文からは外した。カードが大きいので写真も原寸を読む。
    未取得のときだけは空き枠なので、幅58%にとどめて中央に置く。

26. **未取得カードを「番号を振った空き枠」にした（v1.6）。**
    以前はカードの絵を暗くして出していたが、アルバムの台紙のように
    番号が先に並んでいるほうが、何番が揃っていないか分かる。
    `lockedCard()` が `.card--slot` を返す。色はジャンル別（`--slot-c`）。

27. **新しいカードが枠にはまる演出を入れた（v1.6）。**
    取得したカードの番号を `unseenCardIds` に控えておき、次にカード一覧を開いたとき
    空き枠の上にカードが落ちてはまる。はまった枠は一度だけ光る。
    見せ終わったら控えを空にするので、次に開いたときは静かになる。
    「動きを減らす」設定のときは演出せず、控えだけ消す。
    控えを積むのは `rewards.js`（ガチャ）と `geo.js`（現地訪問）の2か所。

28. **一覧の名前にカード番号を添えた（v1.6）。**
    `#01 甘えび` の形。空き枠の番号と並びがそろう。

29. **`el()` がカスタムプロパティを設定できるようにした（v1.6）。**
    `Object.assign(style, {'--i':1})` では CSS 変数は入らない。
    `--` で始まるキーは `setProperty` に回す。演出の順番待ちがこれで効く。

30. **アプリ名を SHIKA COLLECTION にした（v1.7）。**
    `index.html` のタイトルとアプリバー、`js/app.js` の既定タイトル、
    `manifest.webmanifest` の `name`／`short_name`、README の見出し。
    `data/config.json` の `townName`（志賀町）はマップ検索に使う語なので変えない。

31. **枠にはまる演出をゆっくりにし、光を走らせた（v1.7）。**
    0.44秒では気づけなかった。落ちて止まるまでを0.92秒に伸ばし、
    はまったあとに、そのカードだけ左から右へ光が走る（`.cell__drop::after`）。
    1枚ずつ順番にはまる（5枚以下は0.52秒間隔、それ以上は0.36秒間隔）。
    手ごたえ（振動）は「はまった瞬間」に返す。

32. **ガチャ演出の中心を画面のまん中にした（v1.7）。**
    三分のとき3つの束が上半分に固まり、下が余っていた。
    中心を高さの44%から50%に下げ、束の振り分けも縦 ±0.33/0.225H、
    横 ±0.255W に広げた。周回の楕円も縦半径を0.215Hから0.30Hにした。
    札・光の輪・光条の大きさも画面の大きさから決めるようにした。
    **注意**: 中心の値は `js/gacha-anim.js` の `cy()` と
    `css/animations.css` の `top:50%`（カード・紋・光の輪・光条・閃光）の
    両方にある。片方だけ変えると中心がずれる。

33. **カード詳細のカードを端から少し離した（v1.7）。**
    画面の端まで届かせていたが、左右に10pxだけ余白を残す（`margin:0 -6px`）。
    角の丸みも戻した。

34. **ホーム画面の短縮名を「シカコレ」にした（v1.8）。**
    `manifest.webmanifest` の `short_name`。アイコンの下は端末が12文字ほどで
    切るので、正式名（SHIKA COLLECTION）とは別に短いものを置く。

35. **カードに印刷されたボタンを本物のリンクにした（v1.8）。**
    カードの絵はそのまま、`.cardart__btn` の上に透明な `<a>`（`.cardart__hit`）を重ねる。
    行き先は `cardActionUrl()` が決める：スポットは座標の地図、
    グルメは販売店URL→無ければ「町名＋カード名」でマップ検索、
    それも無ければ座標、最後に外部リンク。ボタンが描かれない文化カードには何も足さない。
    印刷どおりの高さ（表示で約23px）は指に細いので、当たり判定だけ上下に広げて約48pxにした。
    カードのそれ以外を押すと、これまでどおり3Dビューアが開く。

36. **カード詳細をスクロールなしで収めた（v1.8）。**
    カードのボタンが地図・販売店検索を担うので、「買える場所」の欄と
    「場所」欄の地図リンクを外した。残すのは現地訪問（SHIKA COIN がもらえる行動）だけで、
    見出しを付けず状況と距離を1行にまとめた。
    そのうえでカードの幅に上限を置く：`calc((100dvh - 390px) * .8)`、
    現地訪問の欄が付くカードは `- 430px`。カードは4:5なので、
    使える高さ × 0.8 が幅の上限になる。
    420×900／390×844／360×740 のいずれでも、3ジャンルともスクロールしない。

37. **枠にはまる演出で、新しいカードを1枚ずつ順番に見せるようにした（v1.9）。**
    以前は最初の1枚だけ画面に寄せていたので、10連だと残り9枚が画面外ではまっていた。
    1枚ごとに「その枠へ寄せる → はめる → 光が走る」を繰り返す。
    - `scrollIntoView({behavior:'smooth'})` は効かない環境があるため、自前で寄せる。
      `requestAnimationFrame` も止まることがあるので、時間で進めるタイマーで動かす。
    - 画面外を省く描画（`content-visibility`）は演出のあいだだけ止める。
      仮置きの高さのままだと、寄せた先が後からずれる。
    - 端末が遅いとタイマーがまとめて遅れて届く。古い順番の指示で引き戻されないよう、
      `turn`（いま見せている順番）と一致しない指示は捨てる。
    - 手で動かされたら（wheel／touchstart／keydown）追いかけるのをやめる。
    - 枚数が多いとき（7枚以上）は1枚あたりを短くして、全部を見せきる。

38. **地図をピンチイン・アウトに対応させた（v1.9）。**
    タイルは整数のズームしか無いので、つまんでいるあいだは
    タイル層とピン層を CSS の transform で拡大し、指を離したところで
    近いズームに寄せて描き直す。つまんだ中心の場所が動かないよう、
    そこが指している緯度経度を覚えて合わせ直す（`pointToLatLng` / `anchorAt`）。
    ホイールでの拡大縮小も、カーソルの下の場所を動かさないようにした。
    2本目の指を離したら、残った指でそのまま地図を動かせる。

39. **カード番号を 001〜053 の連番に振り直した（v1.10）。**
    以前は 1〜57 のうち 35・53・54・55 が欠番で、「#57 / 53」のような表示になっていた。
    並び順（ジャンル順→番号順）のとおりに振り直したので、一覧を上から見ると番号が増える。
    グルメ 001〜032、スポット 033〜048、文化 049〜053。
    `data/cards.json` と `assets/photos/`（`thumb/` も）を同時に書き換えた。
    **番号は利用者の持ちカードを覚えておく鍵なので、公開後は振り直せない。**

40. **メンテナンス用 Excel を作り直した（v1.10）。**
    元の Excel（「カードID」列を持つもの）が手元から失われていたため、
    いまの `cards.json` から `tools/SHIKA_COLLECTION_カード管理.xlsx` を作り直した。
    写真は貼らず「写真ファイル名」列で指し、写真は `assets/photos/` に置く。
    Excel に写真を貼ると巨大になり、それが元データを失った原因でもある。
    変換ツールに読み戻して、いまの cards.json と1件の食い違いもないことを確認済み。

41. **変換ツールが「写真のフォルダ」から写真を取り込めるようにした（v1.10）。**
    Excel のセル内画像に加えて、フォルダから読めるようにした。
    「写真ファイル名」列があればその名前で、無ければ `カードID.拡張子` で探す。
    セル内画像より、フォルダの方を先に見る。`thumb/` のフォルダは取り込まない。

42. **Service Worker が tools/ のページまでアプリ本体に差し替えていたのを直した（v1.10）。**
    画面遷移をすべて `index.html` の控えで返していたため、
    一度アプリを開いた端末では `tools/excel-to-json.html` を開いてもアプリが出た。
    アプリ本体（スコープ直下と `index.html`）だけを控えから返すようにした。
    メンテナンスができなくなる不具合なので、影響は小さくない。

43. **チェックインの文言をそろえた（v1.11）。**
    まち巡りの大きいボタンとカード詳細のボタンを「チェックイン」に。
    確認ダイアログの選択肢も同じ言葉にした。

44. **経路検索（Googleマップ）を足した（v1.11）。**
    `mapsRouteUrl()` が `https://www.google.com/maps/dir/?api=1&destination=緯度,経度`
    を作る。出発地は渡さない（端末の現在地が使われる）。
    アプリ側で座標を保存も送信もしない方針は変えていない。
    置き場所は2か所：まち巡りのスポット行の右端「経路」と、
    カード詳細の「現地に行く」欄（チェックインと横並び。高さを増やさないため）。

45. **はまったあとの光を速くした（v1.11）。**
    0.82秒かけて幅46%の帯を通していたので、靄がかかったように見えていた。
    0.34秒・幅26%の細く鋭い光にした（枚数が多いときは0.30秒）。

46. **管理モード（公開前の点検用）を足した（v1.11）。**
    入り方は「その他」画面いちばん下のバージョン表示を7回続けてタップ。
    確認ダイアログを挟んでから入る。入っているあいだは赤い帯が出続ける。
    - ガチャがコイン無しで引ける（`commitDraw` の `free` 判定に `isAdmin()` を足した）。
      無料10連の消化フラグは、管理モードでは立てない。
    - `#/admin`「カード点検」で全カードの状態を一覧できる。
      公開数・ジャンル別・番号の欠番・写真なし・ジャンル未設定・非公開・
      文が長すぎるもの・チェックイン対象・座標・購入検索・リンク・取得状況。
      1行ずつタップするとそのカードの詳細へ飛ぶ。
    - 全カード取得／取得の取り消し／コイン999／はまる演出をもう一度、の操作。
    - 保存先は `flags.admin`。この端末の保存データを読み書きするだけで、
      アカウントも外部送信も増やしていない。**配布前に切ること。**

47. **Excelの手入れを反映した（データ更新 20260911-4）。**
    変更は9枚ぶん。032「まつり寿司」に説明文を入れて購入検索を外した、
    039・040・042・045・047・048 をチェックイン対象にした（これで全16スポットが対象）、
    050 の名前を「冨来八朔祭礼」から「冨木八朔祭礼」に直した、
    051 の写真を入れた（ファイル名をほかと同じ `.jpeg` にそろえた）。
    tools/excel-to-json.html に Excel と assets/photos を読ませて作り直した。

48. **行き先の無いカードにはボタンを印刷しないようにした（v1.11.1）。**
    ジャンル既定の文言（「取扱店を検索する」など）は行き先があることが前提。
    032 のように購入検索を外した行では、押しても何も起きないボタンが残ってしまう。
    `buttonLabel()` で、グルメは購入検索、スポットは座標が無ければ出さない。
    判定は `cardActionUrl()` と合わせてある。該当は029〜032の4枚。

49. **きらっと走る光を3か所で同じ速さにした（v1.12）。**
    左から右まで0.7秒。カード一覧の「はまったあと」、ガチャの抜刀後、
    3Dビューアの3か所。ビューアは `.card3d__glint` を新しく足し、
    2.6秒の周期のうち先頭0.7秒で走らせて、あとは待つ形で繰り返す。
    指で回したときの光（`--sheen`）とは別の層にして、互いに邪魔しないようにした。
    以前の「3秒かけて右へ、次は左へ戻る」自動光（`startAutoSheen`）は取りやめた。

50. **10連の2枚目以降の速さを半分にした（v1.12）。**
    `speed` を 2.4 から 1.2 へ。あわせて、余韻を待たない回の終わりを
    `T.ui + 260` から `T.ui + 500` に伸ばした。光が途中で切れないため。

51. **枠にはまる演出を作り直した（v1.12）。**
    1枚ずつ順に、画面のまん中へ大きく出して裏面から1回半まわり、
    表を向いてから枠へ吸い込まれて「パチーン」と収まる。
    - 動くのは `.snapfly__c`。外枠 `.snapfly` は位置と奥行き（perspective）だけを持つ。
      こうすると飛び先を「中心どうしの差」で測るだけで済む。
    - 飛び先は `--dx` `--dy` `--k`（枠の幅 ÷ カードの幅）を JS が測って入れる。
    - 枠の側は受け止めるだけ（`snapLand` 0.26秒）。
    - 演出中は背後を少し暗くして、いま見せている1枚が分かるようにする。
      暗幕をタップすると残りをまとめて終わらせる。
    - はまったカードには NEW を付ける。画面を移るまで残るので、
      何が手に入ったかを後から見返せる。

52. **3Dビューアの「真ん中の光のもや」を消した（v1.12.1）。**
    傾きに追従する光沢（`.card3d__sheen`）は、置いてある向きでも正面でも
    帯が真ん中に残り、もやに見えていた。
    しっかり傾けたときだけ濃くする（`|rotY|*0.9 + |rotX|*1.3` が26を超えてから）。
    正面（0,0）でも置いてある向き（REST = x:-8, y:-16）でも0になる目盛りにしてある。
    きらっと走る光は別の層（`.card3d__glint`）が受け持つので、見た目は寂しくならない。

53. **カード詳細の「経路検索（Googleマップ）」を外した（v1.12.1）。**
    カードに印刷されたボタンが同じ役目をしているため。
    まち巡りのスポット一覧の行ごとの「経路」は残してある。

54. **画面を戻ったときに、元の縦位置へ戻すようにした（v1.12.1）。**
    router.js が画面ごとの `scrollY` を覚えておき、戻ったときに合わせ直す。
    画像の読み込みで高さが伸びるので、描いた直後・60ms・220ms・500ms の4回試す。
    はじめて開く画面は覚えが無いので、これまでどおり上から始まる。
    **注意**: 枠にはまる演出は、この戻し（最長500ms）が終わってから動き出すよう
    620ms 待ってから始める。先に動くと、位置の戻しに引っぱられる。

55. **端末に残った古い写真が、別のカードに出ていた不具合を直した（v1.13）。**
    カード番号を 001〜053 に振り直したとき、写真も同じ番号に付け替えた。
    そのため `assets/photos/039.jpeg` のように**ファイル名は同じまま中身だけが別の写真**になった。
    Service Worker の画像の控え（`shika-assets`）はURLで引くので、
    前に見たことのある端末では古い写真が出続けていた。
    症状は「ガチャでは正しい写真、カード一覧では別のカードの写真」。
    一覧は縮小版、ガチャは原寸を使うので、控えの有無で食い違って見える。
    - `activate`（本体の更新時）に `shika-assets` を捨てる。
    - `cards.json` の `dataVersion` が変わったら、アプリから
      `CLEAR_IMAGES` を送って捨てる（`js/update.js` の `clearImageCache()`）。
    - 設定に「写真を読み込み直す」を足した。手元ですぐ直せる。
    **今後の約束**: 写真のファイル名を別の写真に使い回さないこと。
    番号を振り直すときは、`dataVersion` も必ず新しくすること。

56. **Excelの手入れを反映した（データ更新 20260911-5）。**
    18枚ぶん。008に説明文を追加し、17枚の説明文を短く整えた。
    これで全53枚がカードの3行に収まる（あふれるカードはゼロ）。

57. **カード一覧のマスの高さをそろえた（v1.14）。**
    名前が1行のマスと2行のマスで高さが変わっていた。名前の行に2行ぶんの高さ
    （`height:2.7em`）を先に確保する。
    あわせて画面外の仮置き高さ（`contain-intrinsic-height`）を実測の188pxに直した。
    ずれていると、まだ描いていない行だけ高さが違って見える（185px と 188px が混ざっていた）。

58. **カードを開いたときの写真のちらつきを直した（v1.14）。**
    縮小版を出してから `src` を原寸に付け替えていたため、切り替わる瞬間に一度抜けていた。
    付け替えず、原寸を**上に重ねて**読み終わってから不透明度で現す（`layerHi()`）。
    写真・台紙・ロゴの3か所。バッジのアイコンは小さいので縮小版のままにした
    （原寸は1.4MBあり、24pxの表示に読む意味がない）。

59. **カード詳細で左右に払うと前後のカードへ移れるようにした（v1.14）。**
    並びはカード一覧と同じ（ジャンル順→番号順）。端まで来たら反対側へ回る。
    **注意**: 受け付けは `#view` に**1回だけ**付ける。カード詳細を開くたびに付けると、
    開いた回数ぶん重なって何枚も飛ぶ。いま見ているカードはそのつどアドレスから読む。
    速さは見ない（ゆっくり横へ動かしても受け付ける）。
    縦の動きが横より大きいときは何もしない。`touch-action:pan-y` は詳細のときだけ付ける。
    払った直後の指離しで3Dビューアが開かないよう、`justSwiped()` で抑える。

60. **左右に払う操作を、画面の両端では受け付けないようにした（v1.15）。**
    端からの横払いはスマホ本来の「戻る」に使われている。
    左右32pxで始めた操作は無視する（`EDGE`）。

61. **払っているあいだ、前後のカードが見えるようにした（v1.15）。**
    カードの左右に前後のカードを控えさせ（`.cardtrack__side`）、指について一緒に動く。
    離すと、行き先まで送ってから画面を切り替える。戻すときはその場に戻る。
    - 控えのカードはちらりと見えるだけなので、小さい写真で描く。
    - 普段は `display:none`。出しっぱなしにすると画面が横に伸びる。
    - `.detail__cardwrap` を画面幅いっぱいの箱にして `overflow:hidden` で切る。
      こうすると、はみ出した隣のカードが横スクロールを生まない。

62. **枠にはまるとき、必ずカードの表が見えるようにした（v1.15）。**
    表を `rotateY(0)` 側に置き直し、回転は 180°から720°（1回半）にした。
    万一アニメーションが効かなくても表で止まる。
    枠へ飛ぶあいだは720°のまま動かさないので、はまる瞬間まで絵が見えている。

63. **10連で、次のカードへ行くまでの間を2秒のばした（v1.15）。**
    引いたカードを眺める時間がなかったため、`260ms` から `2200ms` に。

64. **アプリバーと同じ見出しを、画面の中で繰り返さないようにした（v1.16）。**
    カード／まち巡り／その他／設定／遊び方／プライバシー／集めた記録／カード点検の8画面。
    その下の一行（「53 / 53 種類」など）は残す。
    画面の中の見出し（`h2`）は、アプリバーに出ていない名前のときだけ置く。

65. **カード詳細の左上に番号を出した（v1.16）。**
    `.detail__no`。カードの金の枠の角に置くので、絵もカテゴリバッジも隠さない。
    表記は一覧やカードの印刷と同じ `#05` の形にそろえてある。

66. **左右に払う操作の、端の除外を広げた（v1.16）。**
    32px では足りず、スマホ本来の「戻る」と競っていた。56px に広げた。
    左右それぞれ 4 / 20 / 40 / 54px から払っても移らず、真ん中からは移ることを確認済み。

67. **起動画面で、青緑色の仮の裏面がちらついていたのを直した（v1.17）。**
    `cardBack()` は「描いた裏面」を先に出し、画像が見つかったら差し替えていた。
    この差し替えが起動直後に見えていた。
    用意した画像（`assets/cards/_back.png`）をそのまま出し、
    読めなかったときだけ描いた裏面に切り替える形に変えた。
    ガチャの円舞や、枠にはまる演出の裏面にも同じ直しが効く。

68. **起動画面からホームに移るときのちらつきを直した（v1.17）。**
    - 順番を入れ替えた。起動画面がまだ出ているうちにホームを組み立て、
      そのあとで起動画面を消す。先に消すと、そのあとで画像を読むことになる。
    - ホームのロゴは原寸が1.8MBある。幅230pxの表示なので、
      小さい方（`frames/thumb/logo.png`）を先に出し、原寸は重ねてから現す。
    - ロゴの場所は `aspect-ratio` で先に取っておく。読み込みで場所が動かない。
    - 起動画面が出ているあいだに、裏面とロゴを読み始めておく（`warmHomeImages()`）。

69. **枠にはまる演出で、裏面が見えないまま表になっていたのを直した（v1.18）。**
    出てくる途中（不透明度0→1）にもう回り始めていたので、
    見えるようになった時点でほぼ表を向いていた。
    裏のまま出てきて、いったん見せてから（30%まで rotateY(180deg) を保つ）
    1回半まわって表で止まる形にした。
    Safari 向けに `-webkit-backface-visibility` と `-webkit-transform-style` も足した。

70. **タブを組み替えた（v1.18）。**
    左から ホーム／まち巡り／カード／ミッション／ガチャ。
    「その他」タブはやめ、アプリバーの歯車（SHIKA COIN の右）から行く。
    ホームはロゴが大きく出るので、アプリバーの見出しは空にした。
    アプリバーのカード枚数の表示もやめた。

71. **ミッションを足した（v1.18）。**
    `js/missions.js`。ガチャを引かないと分からなかった「あと何種類で報酬か」を見えるようにする。
    - 条件はいまの保存データから数え直す。別に進捗を貯めないので、食い違わない。
    - 達成しても勝手にコインは入らない。「受け取る」か「まとめて受け取る」で受け取る。
    - 受け取った記録は `rewardClaims.missions`（ミッションの id の並び）。
    - タブに受け取れる件数を数字で出す（`#missionBadge`）。
    - 中身: ジャンルごとに5種類ずつ／ジャンル全部／カード全部／
      チェックイン1・5・10か所／チェックイン全部。
    **注意**: ジャンル5種類ごとのコインは、これまでガチャの結果画面で自動付与していた。
    二重に渡さないよう、`rewardClaims.category` の回数を受け取り済みのミッションとして
    引き継ぐ処理を `js/storage.js` の normalize に入れてある。

72. **はまる演出の回転を、3Dをやめて作り直した（v1.18.1）。**
    `rotateY` と `backface-visibility` に頼っていたが、端末によっては裏面が隠れず、
    表の絵が左右反転したまま見え続けていた（裏面がいつまでも出てこない）。
    - 回転は **横幅を縮めて0にする**（`scaleX`）形にした。左右反転が起きない。
    - 幅が0になる 38% / 60% / 82% のところで、見せる面を入れ替える
      （`snapFaceBack` / `snapFaceFront` の `visibility`）。
      切り替えは隣り合う目盛り（37.9%→38% など）で一瞬に行う。
    - 裏 → 表 → 裏 → 表 と3回ひるがえって表で止まる。
    - ゆっくり見せたいので、回転の長さを 0.86秒 から 1.5秒（10連は1.15秒）にした。
    確認: 面の移り変わりが 裏→表→裏→表 の順で、
    入れ替わるのは横幅がほぼ0のとき。どの瞬間も左右反転していない。

73. **起動直後にカード一覧から始めたとき、演出が起動画面の裏で進んでいたのを直した（v1.18.1）。**
    v1.17 で「起動画面が出ているうちにホームを組み立てる」ようにしたため、
    カード一覧から始まると起動画面の裏で演出が走り、見えないまま終わっていた。
    起動画面が消えるまで待ってから始める。

74. **画面まわりの書体を、ロゴに合う丸い書体にした（v1.19）。**
    外部フォントは使わない決まりなので、端末に入っているものから選ぶ。
    `--font-ui` に丸ゴシック（ヒラギノ丸ゴ、sans-serif-rounded ほか）を並べ、
    無い端末ではこれまでどおりの書体に落ちる。
    **カードに印刷する文字（`--font`）は変えていない。** 読みやすさを優先する場所なので、
    `css/card-art.css` はこれまでの書体のまま。
    あわせて、角の丸みを14pxから16pxに、ボタンと見出しを少し太くした。
    タブはアイコンを丸い座ぶとんに乗せ、選んでいるものだけ色づくようにした。

75. **右上の設定ボタンを、ちゃんとした歯車のかたちにした（v1.19 → v1.19.2）。**
    中心の円から放射状に線を引いた絵だったので、明るさ調整に見えていた。
    v1.19 で描き直したが、なめらかな塊で歯の形が出ていなかったため、
    v1.19.2 で**角度から計算した8枚歯**の輪郭に置き換えた（32点の多角形）。
    タブの絵と同じく、太い輪郭＋塗り。押すと少し回る。

76. **「集まりぐあい」を「コレクション」としてカード画面の上に移した（v1.19）。**
    ホームからは外し、カード画面のいちばん上に置く。
    これまで別々だった「34 / 53 種類」と、ジャンルごとの棒グラフを1つにまとめた。
    ジャンルの行には、それぞれのアイコンを付けた。

77. **称号にアイコンを付けた（v1.19）。**
    グルメ／スポット／文化はそれぞれのアイコン、志賀町マスターは
    SHIKA COLLECTION のロゴ。未達成のものは色を落として控えめに出す。

78. **タブの絵をトゥーン調に描き直した（v1.19.1）。**
    太い輪郭（`stroke` + `paint-order="stroke"`）に色を塗った、絵らしい形にした。
    ホーム＝赤い屋根の家、まち巡り＝ピン、カード＝重ねたカード、
    ミッション＝チェック入りの星、ガチャ＝カプセルの機械。
    外部のアイコン素材は使わず、index.html の中に直接描いてある（決まりどおり）。
    選んでいないタブは色を落とし（`saturate(.6) opacity(.82)`）、
    選んだタブだけ色が戻って少し浮き、下に短い線が出る。

79. **未取得カードの詳細を、取得済みと同じ形にそろえた（v1.20）。**
    未取得は別のレイアウト（中央に幅58%の枠）だったため、
    左右に払って行き来すると画面が上下に動いていた。
    取得済みと同じ大きさ・同じ位置でカード（空き枠）を出し、
    その下に「???」「まだ持っていないカードです」と「ガチャを引く」を置く。
    中身（旬・場所・リンクなど）は、これまでどおり取得してから出す。
    空き枠は真ん中に大きく番号が出ているので、角の番号は付けない。

80. **何枚めくっても「＜」で元の画面に戻るようにした（v1.20）。**
    払って移るたびに履歴を積んでいたので、「＜」が1枚前のカードに戻っていた。
    払うときは履歴を積まずに**置き換える**（`go(hash, true)`）。
    カードを開く前の画面（カード一覧など）に一度で戻る。
    スマホ本来の「戻る」も同じように働く。

81. **カード一覧の番号を、名前の行からカードの左上へ移した（v1.21）。**
    名前の行に「#01」を並べると、カードに印刷された番号と重なって見えていた。
    カード詳細と同じ、角の小さな札にした。名前の行は名前だけにして中央ぞろえ。
    空き枠は真ん中に大きく番号が出ているので、角には付けない。

82. **称号をカード画面にも出した（v1.21）。**
    歯車 →「その他」→「集めた記録」の奥にあり、見つけられなくなっていた。
    「コレクション」の欄の下に、4つの称号を並べる。

83. **ガチャの途中で画面を離れたら、演出をやめるようにした（v1.21）。**
    離れても演出が裏で回り続け、最後に**別の画面へ結果を書き込んで**いた。
    `hashchange` で演出を止め、離れたときは結果を書き込まない。
    結果自体は残るので、次にガチャを開けば見られる。
    あわせて、前回の結果があるときは「結果を見る」を挟まず、
    ガチャ画面を開いた時点で結果を出すようにした。

84. **効果音をそろえ、はまる音を足した（v1.21）。**
    引いたときの音は、新しいカードでもそうでなくても同じ（`sfx.neu`）にした。
    枠にはまる瞬間に「パチーン」（`sfx.snap`。短い打撃音＋高い余韻）を鳴らす。

85. **タブの文字も絵の中に入れた（v1.22）。**
    `index.html` の svg の中に文字を置き、絵と文字をひとまとまりにした。
    読み上げ用の文字はリンクの `aria-label` に置き、見た目からは隠した。

86. **タブの文字を1画ずつの図形にし、大きさをそろえた（v1.23）。**
    端末の書体に頼るのをやめ、A/C/D/E/G/H/I/M/N/O/P/R/S の13文字を
    幅10・高さ14の枠に線で描き起こした（組み立ては `letters.py`。
    出来上がった道すじを `index.html` に直接書いてある）。
    文字はすべて英語（HOME／MAP／CARDS／MISSION／GACHA）にした。
    1文字の高さは13pxで5つのタブとも同じ。絵の大きさも5つとも84pxでそろえてある
    （選んでいるタブだけ 1.06 倍に持ち上げて見せている）。
    重ね方は裏面のロゴと同じ考え方で、うしろから
    影（`.tabw__s`、下に1.4ずらした薄い黒）→ 輪郭（`.tabw__o`、太い黒）→
    本体（`.tabw__i`、クリーム色）の3枚。選んでいるタブは本体が金色になる。
    **外部フォントは使っていない**ので、どの端末でも同じ形・同じ大きさで出る。

87. **タブを押すと、ぴょんと持ち上がるようにした（v1.24）。**
    押した瞬間（`pointerdown`）に、そのタブだけ `.is-popped` を付けて
    9px 持ち上げ、1.16倍にする（ばね気味の効き方）。
    画面が開いて描き終わったところ（`onRouteChange` の中）で外し、もとの位置に戻す。
    押しても画面が変わらなかったときのために、420ms たてば自分でも戻る。

88. **SHIKA COIN に用意した絵を使うようにした（v1.24）。**
    絵は `assets/icons/coin.png`（192px）と `assets/icons/coin-sm.png`（48px）の2枚。
    小さく出すところは軽い方を読む。組み立ては `ui.js` の
    `coinIcon()`（絵だけ）と `coinAmount()`（絵＋枚数＋SHIKA COIN）にまとめた。
    使っている場所は、上のおびの所持数・集めた記録のいちばん上・
    ガチャ画面の手持ち・ガチャのボーナス合計・ミッションの各行。
    「集めた記録」では、ためたコインを絵つきでいちばん上に大きく出す。
    2枚ともサービスワーカーの本体キャッシュに入れてあるので、
    はじめからオフラインでも出る。

89. **カード番号をカードの上から名前の欄へ移した（v1.25）。**
    一覧は名前の行の先頭に「#01 甘えび」と並べる。
    カード詳細は分類の横に「#01 グルメ」と並べる。
    カードに印刷された絵や番号と重なって読みにくかったのを直した。
    未取得の枠は、中に大きく「???」とだけ出す（番号は名前の行にあるので重複しない）。

90. **称号をミッションの画面へ移した（v1.25）。**
    達成したことが分かるものを1か所に集めた。カード画面のコレクション欄からは外した。

91. **ガチャ画面を作り直した（v1.25）。**
    「ガチャを引く」の見出しをやめ、上からコイン・カードの裏・ボタン2つを積む。
    コインは絵を大きく中央に置き、枚数をその横に出す（SHIKA COIN の字は出さない）。
    カードの裏は、ボタンが入る残りの高さいっぱいまで大きくする。
    たまごが孵る前のように、下を軸にしてゆらゆら揺れる（`eggwobble`）。
    ボタンは「シングルガチャ」「10連ガチャ」。文字は `js/wordart.js` の図形で、
    タブと同じく影・輪郭・本体の3枚重ね。
    カタカナと数字はタブと同じ太さ、漢字（連）は中が潰れないよう細くしてある
    （裏面のロゴが「SHIKA（太い）＋COLLECTION（細い）」で組んであるのと同じ考え方）。

92. **地図を国土地理院から OpenStreetMap に替えた（v1.25）。**
    タイルは `https://tile.openstreetmap.org/{z}/{x}/{y}.png`。
    利用条件により、出典「© OpenStreetMap contributors」を地図の右下に必ず出す。
    サービスワーカーのタイルキャッシュの相手も入れ替えた。最大の拡大は17→18。

93. **まち巡りの一覧の目印を、ピンと「✓」にした（v1.25）。**
    まだの場所はタブの「まち巡り」と同じピン、行った場所はミッション達成と同じ「✓」。
    現在地に近い上位3件は、ピンを少し大きく見せる。

94. **上のおびの「SHIKA COIN」の字をやめた（v1.26）。**
    絵と枚数だけにして、絵と数字を少し大きくした。
    読み上げ用に `aria-label="SHIKA COIN"` を残してある。

95. **チェックインを1回押しただけで、下の一覧が並び替わるようにした（v1.26）。**
    これまでは、チェックインできたときだけ画面を作り直していたため、
    「範囲外でした」で終わると現在地が分かっているのに一覧が古いままだった
    （タブを切り替えると並び替わるのはそのため）。
    一覧を `.spotlists` で囲み、現在地が取れたところで `refreshSpotLists()` で作り直す。
    範囲外のときも、精度が足りなかったときも並び替える。

96. **ガチャ画面の大きさを見直した（v1.26）。**
    画面の高さを `dvh` から `svh`（アドレスバーが出ている状態の高さ）に替えた。
    カードの置き場に `max-height:46svh` を入れ、`.card` の 4:5 の比を外した
    （比が残っていると、高さが横幅から決まって上限を超えてしまう）。
    これでボタン2つは必ず画面の中に収まる。コインの絵は 88→60px にした。

97. **ボタンの値段を「コインの絵＋数字」にした（v1.26）。**
    「1 COIN」「10 COIN」の字をやめ、上のコインと同じ絵と数字で出す。

98. **ボタンの文字を大きく・太くした（v1.26）。**
    線を太くし（カタカナ 4.1→4.6、漢字 1.75→2.05）、
    枠に余白を足して、線が外にはみ出して切れないようにした。
    表示の高さは 21→36px。

99. **称号を 2×2 のバッジにした（v1.26）。**
    ジャンルの絵を 56px の丸いふちの中に置き、名前と達成・未達成を添える。
    達成したものだけ色がつき、金色のふちになる。

100. **ホームから遊び方と設定を外した（v1.26）。**
     どちらも右上の歯車（その他）から行ける。
     残った3つのボタンは、画面の残りの真ん中に置いて釣り合いを取った。

101. **10連はおまけ1枚つきで11枚出るようにした（v1.27）。**
     `gacha.js` の `TEN_BONUS`（いまは1）で枚数を決める。値段は10コインのまま。
     最後の1枚に `bonus: true` を付け、結果一覧で「おまけ」の札と「おまけ 1 枚つき」を出す。
     初回無料の10連は10枚のまま（重複なし・3ジャンル保証のルールも変えない）。
     結果一覧は5枚ずつ並べ、あまりの1枚は真ん中に置く。
     あわせて、管理モードで「1回引く」を押すと10枚出ていた不具合を直した
     （引き方をコインの有無ではなく、ガチャの種類で決めるようにした）。

102. **ガチャのボタンの文字を、ふつうの書体に戻した（v1.27）。**
     1画ずつ描いた文字（`js/wordart.js`）は読みにくかったので廃止し、ファイルも消した。
     値段は「コインの絵＋数字」を名前の右に並べる。
     10連のボタンには、右上に「1枚おトク！ 11枚出ます」の吹き出しをつけた。
     しっぽをボタンに向け、ゆっくり上下に揺らす（動きを減らす設定では止まる）。

103. **まち巡りのボタンを「近くのスポットを探す」にした（v1.27）。**
     押したときの動き（現在地を取って、範囲内ならチェックイン）は変えていない。
     カード詳細のスポット欄の「チェックイン」はそのまま。

104. **ガチャ画面のコインを、画面の真ん中に置いた（v1.28）。**
     コインと枚数をまとめて中央に寄せると、コインが左にずれていた。
     「左の余白・コイン・右の余白」の3列にし、コインを真ん中の列、枚数を右の列に置く。
     枚数の桁が増えても、コインの位置は動かない。

105. **タブの順番を「まち巡り・カード・ホーム・ミッション・ガチャ」にした（v1.28）。**
     `index.html` のタブの並びを入れ替えただけ。順番に頼っている処理はない。

106. **ホームに「SNSでシェア」を足した（v1.28）。**
     `js/share.js`。スマホの共有機能（`navigator.share`）があれば、それを開く。
     ない端末では、X／LINE／Facebook の投稿画面へのリンクと「リンクをコピー」を出す。
     外部のSDKやタグは読み込まない。押したサービスへ移動するときだけ通信する。
     送るのはアプリのURL（#以降と ? 以降は外す）と、集めた枚数の一文だけ。

107. **起動演出を、増穂浦のオープニングに差し替えた（v1.29）。**
     別PCで作った `SHIKA_OPENING_TRANSFER_1.17.10` から、オープニングだけを差分で取り込んだ。
     既存の画面・ルーティング・カードデータ・カードデザインは変えていない。
     - 本体は `js/opening.js` と `css/opening.css`（移植元から無加工でコピー）。
       `opening.css` は既存のカードCSSより後に読む。
     - 同梱の絵18枚は、すでにこのプロジェクトにあるものと中身が同じだったので、コピーしていない。
     - 中央は既存のカード裏面をそのまま出し、周りの8枚（003・024・035・036・038・049・053・050）は
       `cardFace()` で作る。カードの比率・金縁・番号・写真・説明・カテゴリはそのまま。
     - `sound.js` に `isUnlocked()` と、起動用の効果音8つ（`openingWake` など）を足した。
       音の設定がONのときだけ鳴る。操作前は鳴らさず、左上のボタンが「音を有効にする」になる。
     - `storage.js` の既定値に `flags.openingPlayed` を足した。
       1回目は長い版（full）、見終わるかスキップしたあとは短い版（short）。
       端末が動きを減らす設定なら簡易版（reduced）。
     - `app.js` の起動は、演出を作る → 必須の絵とカードデータを並行で読む →
       周りのカードを渡す → 開始 →「スタート」か「スキップ」を待つ → 起動画面を消す、の順。
       2.2秒以内にそろわない初回は短い版にする。
       ホーム画面は起動画面のうしろで先に組み立てておく（ちらつき防止のため、これまでどおり）。
     - 旧い起動演出（カードの裏を5枚広げる `playIntro`）と、そのCSS・動きの定義は外した。
     - 確認用に `?opening=full` `?opening=short` `?opening=reduced` で種類を固定できる。
       このときは「見た」記録を残さない。

108. **起動演出のロゴを上げた（v1.29.1）。**
     `css/opening.css` の `.opening__logo` に `top:-10dvh` を足し、画面の高さの10%ぶん上に置いた。
     絵の上端は、画面の上から約36%の位置から約26%の位置になる。
     ロゴの飛び出す動きは transform で付けているので、位置は top でずらして干渉させていない。
     「スタート」の位置は変えていない。

109. **起動演出の桜貝の飾りを外した（v1.29.2）。**
     画面の下のほうに5つ置いていた小さな桜貝（`.opening__shell`）が、
     「スタート」のまわりで邪魔に見えたため。
     `opening.js` の組み立てと光らせるタイマー（`beginShellGlow`）、
     `opening.css` の見た目・動きの定義をまとめて外した。背景の増穂浦の写真はそのまま。

110. **カードタブで写真が黒く抜ける／10連の準備が長い、を直した（v1.29.3）。**
     回線をスマホ並み（下り1.6Mbps）に絞って測った。
     - カードタブ：開いた瞬間、画面内9枚のうち8枚の写真がまだ無く、黒い下地が見えていた。
       - 起動後（カードデータを読んだあと）に、小さい写真53枚と台紙・アイコンの縮小版を
         裏で読んでおくようにした（`app.js` の `warmCardThumbs`。合計およそ1.3MB、同時2本、優先度低め）。
       - 写真の下地を黒から明るい砂色にし、小さい写真は読み終わってからふわっと現す
         （`card-render.js` の `cardart__pic`。すでに手元にあるときは最初から出す）。
         「？」と「写真準備中」は白い文字なので、下地は暗いまま。
     - 10連：「カードを準備しています…」が約8秒続いていた（待ち時間の上限に当たっていた）。
       - 演出前に原寸の写真11枚（平均640KB・最大3MB）と、原寸の台紙・アイコン（1枚1MB超）まで待っていた。
       - 待つのはカードの裏と、小さい写真・台紙・アイコン・ロゴだけにし、上限も8秒→1.5秒にした。
         原寸は待たずに裏で読み始める。カードは小さい写真の上に原寸を重ねて、
         読み終わったら現す作りなので、見た目は途中からくっきりする。
       - 原寸を小さい部品と同時に読み始めると、ブラウザの同時接続（1サーバー6本ほど）を
         原寸が占めて、小さい写真が後ろで待たされた。小さい部品がそろってから、原寸を2本ずつ読む。
     - あわせて、初めて開いたときに数秒後に勝手に開き直る不具合を直した。
       Service Worker が初めて入ると、その場で controllerchange が起き、
       `update.js` がそれを見て無条件に `location.reload()` していた。
       起動演出がもう一度流れたり、最初の10連の途中で画面が戻ったりする原因になっていた。
       「新しいバージョンがあります」で「更新する」を押したときだけ開き直すようにした。

111. **称号「志賀町コンプリート」を足した（v1.30）。**
     条件は、すべての公開カードを取得し、すべてのチェックイン対象スポットを訪問済みにすること
     （`rewards.js` の `isTownComplete()`）。
     - 絵は `assets/icons/title-complete.png`。支給画像は背景の市松模様が絵として描き込まれていたので、
       ふちから灰色の画素だけを辿って消し、512px の正方形にした。
     - ミッション画面の称号欄で、4つの称号の下の真ん中に置く。名前は最初から出し、
       絵は獲得するまで「？？？」にする。
     - 獲得の演出は `js/title-complete.js`。条件を満たした瞬間に1回だけ出す
       （`flags.completeCelebrated`）。ガチャの演出中・結果の確認中・起動画面やお知らせの表示中は待つ。
       ミッション画面へ移り、画面を暗くして「？？？」が飛び出し → 回って表に → 光と「称号獲得！」 →
       称号の枠へ飛んでいき、パチーンとはまる。タップで最後まで飛ばせる。
     - きっかけは、画面を移ったとき・起動画面が閉じたとき・チェックインが終わったとき。
     - 管理モードに「コンプリート演出を見る」（保存データを変えずに演出だけ流す）と
       「コンプリート状態にする」（全カード取得＋全スポット訪問にして、本番と同じ流れで演出を出す）を足した。

112. **下のタブで開く画面では、左上の「＜ 画面名」を出さないようにした（v1.30）。**
     ホーム・まち巡り・カード・ミッション・ガチャが対象。
     カード詳細や歯車の奥の画面など、戻る先がある画面では出す。

113. **地図で訪問済みの場所を、緑のピンから金の星にした（v1.30）。**

114. **ミッション画面の並びを 称号 → まとめて受け取る → カード → まち巡り にした（v1.30）。**

115. **ホームを「最近のカードを大きく見せる」画面にした（v1.30）。**
     持っているカードを1枚ずつ、ガチャ画面のカードと同じ大きさ（画面の高さの46%まで）で出す。
     最初はいちばん新しく手に入れたカード。以後10秒ごとに入れ替え、
     新しい順の上位5枚は4倍、次の10枚は2倍出やすくする（同じカードは続けて出さない）。
     押すとそのカードの詳細へ。1枚も持っていなければカードの裏を出し、押すとガチャへ。
     ほかのボタン（ガチャを引く・カードを見る・まちを巡る）はいったん外し、SNSでシェアだけ残した。

116. **ホームからロゴを外し、カードの入れ替えを5秒にした（v1.31）。**
     ロゴは起動演出で大きく見せているので、ホームは上のひとことと、カードと、SNSでシェアだけにした。

117. **ミッションの報酬を受け取ると、コインが右上へ飛ぶようにした（v1.31）。**
     `js/coin-fly.js`。押したボタンの所から、コインが少しずつ間をあけて右上の枚数表示のコインへ飛ぶ。
     同じ道すじを続けて飛ぶので、飛んでいるあいだに一直線の列になる
     （ボタンの所に並べてから飛ばすと、ボタンが画面の下にあるとき列が画面の外へはみ出した）。
     1枚届くたびに枚数がメーターのように増え、
     右上の表示がぽんと弾む。受け取った枚数ぶん飛ぶ（20枚まで。それ以上は1枚あたりの増え方で合わせる）。
     「受け取る」「まとめて受け取る」の両方。続けて押すと、前の演出が終わってから次が始まる。
     飛んでいるあいだは `#statCoins` に `data-hold` を付け、`app.js` の表示更新が数字を書き換えないようにしている。
     動きを減らす設定では飛ばさず、数字だけを手早く増やす。

118. **「志賀町コンプリート」の演出で、枠にはまるまで照らし続けるようにした（v1.31）。**
     これまでは称号が枠へ飛び始めた瞬間に暗い幕が消え、画面全体が見えてしまっていた。
     飛んでいるあいだも幕は下ろしたまま、称号の枠のところだけを丸く照らす。
     はまったあとも約1秒照らしたままにしてから幕を上げる。

119. **ガチャの SKIP が1回で効かないことがあったのを直した（v1.31）。**
     - 原因1：カードとカードの間の待ち（2.2秒）が途中で切れず、SKIP を押してもしばらく何も起きなかった。
     - 原因2：SKIP のあとも、最後の1枚は演出を最後まで流していた。
     どちらも「効いていない」ように見え、もう一度押す必要があった。
     SKIP で待ちを起こし、最後の1枚も演出を流さずに最終状態にしてから結果へ進むようにした。

120. **カード左上のジャンル表示（紺のピル）の幅をそろえた（v1.31）。**
     左右の余白が端末の文字の大きさ（16px）で決まっていて、小さいカードほど右に大きく伸びていた
     （幅110pxのカードでは右の余白がカード幅の22%）。
     余白と間をカードの幅に対する割合（cqw）で決め、いちばん長い「スポット」が収まる幅に固定し、
     中身は真ん中にそろえた。グルメ・文化も同じ幅で、左右の余白が等しくなる。

121. **初回のホームに、ロゴと「無料10連」のポップを出すようにした（v1.32）。**
     無料10連をまだ引いていない人（`flags.firstFreeTenDone` が false）だけ。
     - 上に SHIKA COLLECTION のロゴ（小さい方を先に出し、原寸は読み終わってから重ねる）。
     - カードの代わりに、金色のポップ。左上に「初回限定」のリボン、裏向きのカード3枚、
       大きな「無料」「10連ガチャ」、「タップして引く ▶」。弾むように揺れ、ときどき光が横切る
       （動きを減らす設定では止まる）。押すとガチャ画面（無料で10連を引くボタン）へ。
     - 無料10連を引いたあとは、これまでどおりロゴ無しで、持っているカードを大きく見せる。

122. **「志賀町を、あつめよう。」をロゴ風にした（v1.32）。**
     裏面のロゴの COLLECTION と同じく、両端を切り欠いた青いリボンに、ふちどりの太い文字をのせた。
     「志賀町」は金色、「を、あつめよう。」は白。少し傾け、下に影を落とす。
     文字は画像ではなく本物の文字なので、読み上げにもそのまま使える。

123. **「無料」の文言をやめ、初回のガチャ画面にもカードの裏を出した（v1.33）。**
     - ホームのポップは「初回限定」「10連ガチャ」「タップして引く ▶」だけにした（「無料」を外し、10連ガチャを大きく）。
     - ガチャ画面の初回は、いつものガチャ画面と同じく、カードの裏を大きく出してゆらゆら揺らす。
       上に「はじめまして」のひとこと、下に「10連ガチャを引く」ボタン。
     - 設定のリセット確認と遊び方の説明からも「無料」を外した（「初回の10連」「まず10連から」）。

124. **「志賀町を、あつめよう。」のロゴを作り直した（v1.33）。**
     v1.32 の青いリボンは野暮ったかったので、4案を描き比べて選び直した。
     ゆるい弧に沿って並べた白い文字に青いふちを付け、両脇に金の星を置く
     （裏面のロゴの COLLECTION と同じ配色）。SVG の textPath で文字を弧に沿わせている。
     文字は画像ではなく本物の文字なので、読み上げにもそのまま使える。

125. **「志賀町を、あつめよう。」を支給のロゴ画像に差し替えた（v1.33.1）。**
     v1.33 で描いた弧の文字をやめ、支給画像を使う。画像はもともと背景が透明だったので、
     透明な余白だけを切り落とし、`assets/icons/tagline.png`（横900px）と
     `assets/icons/tagline-sm.png`（横360px）の2つにした。
     小さい方を先に出し、原寸は読み終わってから重ねて現す（SHIKA COLLECTION のロゴと同じやり方）。
     読み上げ用に `alt="志賀町を、あつめよう。 SHIKA COLLECTION"` を付けた。
     2つともサービスワーカーの本体キャッシュに入れたので、はじめからオフラインでも出る。

126. **初回のホームから SHIKA COLLECTION のロゴを外し、オープニングの「スタート」を白文字にした（v1.33.2）。**
     - 初回のホームは「志賀町を、あつめよう。」のロゴ（中に SHIKA COLLECTION も入っている）と、
       「10連ガチャ」のポップ、SNSでシェアだけにした。見出しのロゴは、ほかのホームと同じ大きさに戻した。
     - オープニングの「スタート」は、文字を白に。枠の内側の上に入れていた白い光（立体感の演出）は、
       枠と同じ金で塗りつぶした（押したときの見た目も同じ）。

127. **ミッション画面の称号を、いちばん下へ移した（v1.34）。**
     並びは まとめて受け取る → カード → まち巡り → 称号。

128. **まち巡りとミッションを初めて開いたときに、案内を出すようにした（v1.34）。**
     `js/guide.js`。カードを見るときの案内と同じく、動く絵・見出し・説明を重ねて出す。
     説明が長めなので自動では消さず、画面をタップすると閉じる。1回だけ
     （`flags.mapGuideShown` / `flags.missionsGuideShown`）。
     起動画面・ガチャ・称号の獲得演出・お知らせが出ているあいだは待ち、
     待っているうちに別の画面へ移ったら出さない（記録もしない）。

129. **ホームのカードを大きくし、SNSでシェアをカード画面へ移した（v1.34）。**
     「志賀町を、あつめよう。」のロゴを小さく（横230pxまで）して上に寄せ、
     ホームを画面の高さぴったりにして、残りの高さをぜんぶカードに使う。
     SNSでシェアは、カード画面のコレクション欄の下に
     「◯種類あつめたことを、みんなに知らせよう」の一文と一緒に置いた。

130. **案内の説明文を左揃えにし、ホームのロゴを画面の端まで広げた（v1.34.1）。**
     - 初回の案内（`js/guide.js`）は、説明文だけ左揃え。見出し・絵・「タップして閉じる」は真ん中のまま。
     - 「志賀町を、あつめよう。」のロゴは、画面の左右の端から4pxまで広げた
       （`.view` の左右の余白を打ち消している）。カードはその下の残りの高さに入るだけ大きく出す。

131. **リセットしたあと、ログインボーナスがもらえなかったのを直した（v1.34.2）。**
     - 原因：ログインボーナスはアプリの起動時に1回だけ受け取る作り。ところがリセットは
       保存データを初期値に戻して画面をホームへ移すだけで、起動し直していなかった。
       そのため、次にアプリを開き直すまでボーナスが入らなかった
       （ホームには「受け取れます」とだけ出て、受け取る手段が無かった）。
     - リセットのあとは、開き直して初めて開いたときと同じ流れ（起動演出 → ログインボーナス → 初回の10連）にした。
     - 開いたまま日付が変わった場合にも受け取れるよう、画面を移ったときと、
       アプリに戻ってきたとき（visibilitychange）にも確かめるようにした。起動画面の最中は待つ。
     - ボーナスは自動で受け取って通知で知らせるので、ホームの「受け取れます」の一文は外した。

132. **カード一覧で説明文が途中で切れていたのを直した（v1.34.3）。**
     一覧の小さいカード（幅150px以下）だけ、説明文を2行で打ち切る指定が残っていた。
     説明文は最大3行なので、3行あるカード（53枚中12枚）が一覧だけ途中で切れていた。
     この指定を外し、一覧でも詳細と同じく3行ぶん全部を出す。
     文字の大きさも枠の高さも cqw（カード幅に対する割合）で決めているので、
     小さいカードでも3行がそのまま収まる。

133. **ホームのカードの説明文が中央ぞろえになっていたのを直した（v1.34.4）。**
     ホームのカードは押せるように `<button>` の中に置いている。ボタンは文字を中央ぞろえにするので、
     それがカードの説明文にまで引き継がれていた。
     `.cardart` で、まわりから引き継ぐ書式（そろえ・字間・行間・色など）を決め直し、
     一覧のマス・ホーム・ガチャの結果など、どこに置いてもカードタブと同じ見た目になるようにした。

134. **「プライバシーについて」の入口を1つにした（v1.34.5）。**
     「その他」の一覧に加えて、設定画面と遊び方の画面にも同じ入口があり、重なって見えていた。
     「その他」の一覧の1つだけを残し、設定画面と遊び方の画面からは外した。

135. **初めて開いたあと、通信が切れると起動できなかったのを直した（v1.35、レビュー F01）。**
     - 原因：起動時に公開データ（cards.json など）を読んだあとで Service Worker を登録していた。
       Service Worker が入るときに控えるのはアプリ本体だけで、公開データは控えていなかった。
       そのため初めて開いたときは公開データの控えが無く、通信が切れると「データを読み込めませんでした」になった。
     - Service Worker が入るときに、version.json・cards.json・config.json も控える
       （通信優先で読むときと同じ名前で置くので、通信が切れたときにそのまま使われる）。
     - Service Worker は起動のいちばん最初に登録する（スタートを押す前に閉じても、控えが間に合う）。
       更新の案内は、起動画面やほかのお知らせが閉じるまで待ってから出す。

136. **保存に失敗しても、獲得できたように見えていたのを直した（v1.35、レビュー F02）。**
     - 原因：`commit()` は先に状態を書き換えてから保存し、保存の成否を見ていなかった。
       容量不足などで保存に失敗しても、その場ではカードやコインが増え、開き直すと元に戻っていた。
       ガチャは消費・獲得・結果を3回に分けて保存していた。
     - `commit()` / `setState()` は、状態の写しを変更して保存し、保存できたときだけ本物と入れ替える。
       失敗したら状態は元のまま（`saveOk()` で成否が分かる）。
     - `storage.save()` は 'ok'／'memory'（localStorage が使えない端末。これまでどおりメモリ上で続ける）／
       'failed'（書き込めなかった）を返す。書き込みに失敗したあとは `isPersistent()` も false になる。
     - ガチャは、コインの消費・カードの獲得・ボーナス・未確認の結果を1回の保存にまとめた。
       保存できなければ引かなかったことにし、演出も始めない。
     - ミッションの受け取り・ログインボーナス・チェックイン・コインの付与も、保存できなかったときは
       成功の知らせを出さず、コインも増やさない。
     - 失敗したら「保存できませんでした」を出し、設定（バックアップ）へ案内する。
       ほかのお知らせや演出の最中は、それを消さないよう画面下の知らせにする。続けて失敗しても8秒に1回まで。

137. **カードが枠にはまる演出が、画面を移っても続いていたのを直した（v1.36、レビュー F03）。**
     画面を離れたとき（タブの移動・カードを開く・絞り込みで描き直す）は、演出をそこでやめ、
     暗い幕と飛ぶカードを片付ける。離れた画面を勝手に動かさない。
     「見た」にするのは、実際に枠にはまったカードだけにした（以前は途中でやめても全部消していた）。
     残りは、次にカード画面を開いたときに演出する。

138. **横向きのカード詳細でカードが消えていたのを直した（v1.36、レビュー F04）。**
     カードの幅を「画面の高さ − 390px（スポットは430px）」から決めていたので、高さ390px以下で0になっていた。
     高さ600px以下では、横幅（62vw、最大260px）でカードの大きさを決め、縦に送って見られるようにした。

139. **config.json のミッション報酬が無視されていたのを直した（v1.36、レビュー F05）。**
     設定の取り込み（`withConfigDefaults`）に `mission` を加えた。
     categoryStep・categoryAll・allCards・visit・visitAll を0以上の数だけ受け付け、ほかは既定値にする。

140. **壊れたバックアップの中身を確かめるようにした（v1.36、レビュー F06）。**
     復元の前に、カード・気になる・コイン・かぶりゲージ・訪問・設定・ガチャの結果の形を確かめ、
     1つでもおかしければ復元しない（いまの進行は置き換えない）。
     ガチャの結果は `isValidPendingResult()` で、種類・結果の並び・カードID・ボーナス明細の型と範囲まで見る。
     保存データを読むときと結果画面を出すときにも同じ確認をし、壊れた結果は捨ててふつうのガチャ画面にする。

141. **一部のファイルが取れなくても更新を成功扱いにしていたのを直した（v1.36、レビュー F07）。**
     Service Worker が入るとき、HTML・JS・CSS・manifest のどれか1つでも取れなければ、入るのを失敗させる。
     作りかけの控えは消し、いまの版をそのまま使い続ける。絵は見るときに取り直せるので、欠けても進める。

142. **スポットのカードのボタンが、経路ではなく場所の検索を開いていたのを直した（v1.36、レビュー F08）。**
     ボタンの文言（Googleマップで経路を見る）に合わせて、経路の検索（`mapsRouteUrl`）を開く。
     行き先の座標だけを渡し、現在地は渡さない。

143. **カードが枠にはまる演出に SKIP ボタンを付けた（v1.37）。**
     演出のあいだだけ右上に SKIP を出す（ガチャの SKIP と同じ見た目）。暗い幕を触っても同じ。
     押すと、回っている途中・飛んでいる途中でもすぐに片付け、残りのカードをまとめて枠に収めて NEW を付ける。
     収めたカードは確認済みにする。画面を離れたときは今までどおり収めない。

144. **ホーム画面に追加（PWA）のおすすめを、初めて開いたときに出すようにした（v1.37）。**
     以前は初回の10連を引いたあとに出していた。起動演出を閉じてすぐ、1回だけ出す。
     すでにホーム画面から開いているときは出さない。
     ブラウザが追加の画面を用意しているとき（Android の Chrome など）は「ホーム画面に追加」ボタンから直接追加できる。
     用意がないとき（iPhone の Safari など）は、端末に合わせた手順を出す。

145. **カード一覧に「すべて・取得済み・未取得」の絞り込みと、カード名の検索を付けた（v1.38、レビュー案5）。**
     ジャンルのタブの下に置き、ジャンルと組み合わせて使える。選んだ条件は画面を移っても覚えておく。
     選んだり打ったりしたときは一覧だけを描き直し、入力欄は作り直さない（打っている途中で切れないように）。
     検索は全角・半角、大文字・小文字、カタカナ・ひらがな、空白の違いを無視する。日本語の変換中は確定を待つ。
     まだ持っていないカードの名前（一覧で伏せている名前）では探せない。訪問で公開しているスポットは探せる。
     枠にはまる演出は、絞り込みで見えているカードだけに出し、見えていないカードは次に見えたときに出す。

146. **位置情報を確認できなかったときの案内を、理由ごとに分けた（v1.38、レビュー案6）。**
     以前はどの理由でも「屋外など、空が見える場所で再度お試しください」だった。
     - 許可されていない：iPhone・Android・そのほかで設定の手順を出す（試し直しても同じなので「閉じる」だけ）
     - 見つからない：端末の位置情報（GPS）がオンか、建物の中ではないか。「もう一度試す」
     - 時間切れ：少し待ってから。「もう一度試す」
     - 精度が足りない：いまの精度と、必要な精度（config.json の gpsAccuracyLimit）を出す。「もう一度試す」
     理由は `geo.errorKind()` がブラウザの理由の番号（1=許可なし 2=見つからない 3=時間切れ）から決める。

147. **大きく出す画像を、表示用の軽い WebP にした（v1.38、レビュー案8）。**
     測った量（Service Worker と控えを使わず、画面の細かさ3倍）：
     初めて開いてホームまで 6.81MB、初回の10連 6.08MB（原寸の写真10枚で4.9MB）、カード詳細1枚 1.30MB。
     大きいのは、ロゴ（1.8MB）、カードの裏（1.8MB）、「志賀町を、あつめよう。」（0.8MB）と、原寸の写真だった。
     - 写真：カードの写真の窓（1080の設計で965×623）を、画面の細かさ3倍の端末でも粗くならない 1100×710 を覆う大きさまで縮める
     - 台紙・ロゴ・カードの裏・「志賀町を、あつめよう。」：透過を保ったまま WebP にする
     - 置き場所は各フォルダの web/。アプリは web/ を先に読み、無ければ元の画像を読む（元の画像は残す）
     - 変換ツール（tools/excel-to-json.html）の「写真を書き出す」で、photos.zip に web/ も入る
     - 一覧用の小さい写真（thumb/）は、もともと1枚20KBほどなのでそのまま
     - 変換した59件で 39.2MB → 5.2MB

148. **カード名の検索で、濁点・半濁点の違いも無視するようにした（v1.38.1）。**
     「カニ」で「加能ガニ」が見つかるようにする（以前は「ガニ」と打たないと見つからなかった）。
     文字を「か」＋濁点のように分けてから濁点・半濁点を外して比べる。「はし」で「ばし」「ぱし」も見つかる。

149. **増穂浦海岸（035）と高爪山（040）の緯度経度を直した（データ 20260914-1）。**
     カード管理の Excel で直した値を data/cards.json に入れ、tools/ の Excel も差し替えた。
     - 増穂浦海岸：37.150038,136.707409 → 37.13942168208599,136.72402758326442
     - 高爪山：37.206952,136.724368 → 37.19203617709108,136.717235868344
     地図のピン、近い順の並び、チェックインの判定、カードの経路ボタンは、すべてこの値から決まる。
     増穂浦海岸の新しい位置は、道の駅 とぎ海街道（036）から約130mで、どちらも判定半径200m。
     重なる場所でチェックインすると、2か所まとめて訪問になる。

150. **カードの名前の文字の大きさを、すべてのカードでそろえた。説明文を太字から通常に戻した（v1.39）。**
     以前は名前の文字数で大きさを3段階に変えていた（9文字まで 6.5cqw、13文字まで 5.1cqw、それより長いと 3.7cqw で2行）。
     カードによって名前の大きさがばらばらで、見づらいという指摘があった。
     いまの名前はいちばん長くて12文字（こぼりのチーズまんじゅう）で、6.5cqw でも1行に収まるので、全カードを 6.5cqw にした。
     13文字以上の名前が入ったときだけ、そのカードは1行に収まる大きさまで縮める（変換ツールが警告を出す）。
     説明文は font-weight 700 → 400。

151. **4枚のカードの名前を変えた（データ 20260915-1）。**
     カード管理の Excel で変えた名前を data/cards.json に入れ、tools/ の Excel も差し替えた。
     - 030 お汁（お祭り） → お汁
     - 042 日本遺産　北前船寄港地福浦港 → 福浦港
     - 044 道の駅　ころ柿の里しか（みちのえき旬菜館） → 道の駅　ころ柿の里しか
     - 048 大島キャンプ場（海水浴場） → 大島キャンプ場

152. **ホーム画面に追加したときの名前を「シカコレ」から「しかコレ」に変えた（v1.39.1）。**
     Android などは `manifest.webmanifest` の `short_name` を使う。
     iPhone は manifest ではなくページの `apple-mobile-web-app-title` を使うので、index.html にも同じ名前を入れた。
     すでにホーム画面に置いてある名前は変わらない（端末が追加したときの名前を持ち続けるため）。置き直すと新しい名前になる。

153. **「カードを見る」の3Dビューアでも、カードに印刷されたボタンからGoogleマップなどを開けるようにした（v1.39.2）。**
     以前はカード詳細の中だけで押せた。3Dビューアはカードを指で回す操作が指を捕まえるので、ボタンが押せなかった。
     - 行き先の決め方と、押せる場所を重ねる処理を js/card-link.js にまとめ、詳細と3Dビューアの両方で使う
     - ボタンに触れたときは回す操作を始めず、そのままリンクを開く（ボタン以外からは今までどおり回せる）
     - 裏面を向いているときは、表のボタンは見えていないので開かない
     - 3Dビューアはカード詳細・ガチャの結果・地図から開くので、どこから開いても押せる

154. **称号とカードを、画像つきでSNSにシェアできるようにした（v1.40）。**
     - 称号：ミッション画面で獲得した称号のメダルに「シェア」。メダルと称号の名前を描いた 1080×1080 の絵
     - カード：カード詳細（持っているカード）に「シェア」。画面のカードと同じ部品を 1080×1350 の絵に描く
     - 絵は js/share-image.js がこの端末の中で描く（外部には送らない）。文にはアプリのトップページのURLを入れる
     - 共有機能で絵を渡せる端末は、絵・文・URLをそのまま共有画面へ。
       絵を作っているあいだに共有の許可が切れた端末は、絵を見せて「画像つきで共有する」をもう一度押してもらう
     - 共有機能の無い端末は、絵の保存と、X／LINE／Facebook（文とURL）、リンクのコピーを出す
     - 押してから描くと遅いので、画面を開いて少ししたら裏で描いておく

155. **まち巡り画面のいちばん上の文言を「現地訪問 0 / 16 か所」から「石川県志賀町」にした（v1.40）。**

156. **ログインボーナスを広げた（v1.40）。**
     毎日 +1 に加えて、ログインした日数が 5日目 +5 ／ 10日目 +10 ／ 15日目 +15。15日目の翌日からまた1日目。
     日数は受け取った日を数える（続けてでなくてよい）。設定は config.json の coin.loginBonus。
     ミッション画面のいちばん上に「ログインボーナス」を置き、SHIKA COLLECTION のロゴを色の無い版から、
     日数のぶんだけ左から色を付けて見せる。今日はじめて開いたときだけ、前の日の位置から今日の位置まで色が伸びる。

157. **コインが足りないときのガチャ画面で、カードの裏を少し小さくし、「10連まであと少し」の見出しが下に見えるようにした（v1.40）。**

158. **まち巡り画面の「石川県志賀町」の下のバーを外した。管理モードにログインボーナスの確認を足した（v1.40.1）。**
     管理モードの「ログインボーナスの確認」：
     - 翌日にする（受け取る）… 今日の受け取りの記録を消して、翌日に開いたのと同じ流れで受け取り、ミッション画面へ
     - 日数を合わせる … 4日目まで／9日目まで／14日目まで／15日目（1周）／0日に戻す。今日の分は受け取り済みにする
     - 色が付く演出をもう一度 … 演出を見せた記録を消して、ミッション画面へ
     変わるのは、この端末の保存データだけ。

159. **ログインボーナスのロゴを、1日1文字ずつ色付けにした。15日目に達成の演出を入れた（v1.41）。**
     1日目「S」、2日目「H」…15日目「N」と、SHIKA COLLECTION の15文字に1日ずつ色が付く。
     ロゴは1枚の絵なので、点ごとに文字の番号を書いた区分け地図（assets/frames/web/logo-letters.png）を作った。
     文字の明るい面（SHIKA は黄色、COLLECTION は白）を目印にし、ほかの点は直線でいちばん近い文字の面に振り分けた
     （金のふちは SHIKA の面、青い帯は COLLECTION の面から選ぶ）。
     画面ではロゴの点を読み、色を付ける文字は元の色、ほかは色の無い薄い姿で canvas に描く。
     - 今日の文字：光りながら大きめに出て、ぽんと収まる。まわりに小さな光が散る
     - 目盛り：点ではなく「S H I K A C O L L E C T I O N」の15文字。+5／+10／+15 の日は枠つき
     - 15日目：最後の文字のあと、ロゴが縮んでから弾んで跳ね、後ろに金色の光、ロゴの形に光の筋、
       キラキラが飛び散り、「15日コンプリート！」が弾んで出る
     - 動きを減らす設定の端末では、動かさずに結果だけを出す
     - 区分け地図を読めないときは、以前の見せ方（左から日数の割合ぶん）に戻す

160. **ホームの「志賀町を、あつめよう。」のロゴを、動画に替えた（v1.42）。**
     志賀町の風景と SHIKA COLLECTION のロゴの動画を、音なし・ループで流す。
     いつもの人は 16:9、初めての人は下が空いているので 4:3 で大きく出す（はみ出た左右を切る。ロゴは真ん中なので切れない）。
     ループは、最後の0.6秒で最初の場面の絵に溶けてから最初に戻るので、つなぎ目が見えない。
     元の動画（1280×720・38秒・約35MB）を、上下の黒い帯と最後の黒い所を切り、音を外し、960×408・約0.75Mbps の H.264（約3.5MB）に作り直した。
     長さと目次をファイルの先頭に置いた普通の MP4 にした（細切れ形式だと長さが記録されず、読み始めが遅い・iPhone でループしないおそれがある）。
     重くしないための工夫：
     - 起動画面のあいだは読まない。起動画面が消えてから読み始める。読むまでは最初の場面の絵（約80KB）
     - ホームを離れたら止めて読み込みも手放す。アプリを閉じたら止め、戻ったら続きから
     - 動きを減らす設定・データセーバーの端末は絵だけ
     - Service Worker では控えない（動画の途中から読む要求を控えから返すと、iPhone で流れないことがあるため）
     測った重さ（処理の速さ 1/4・回線 4G 相当 4Mbps、Service Worker なし）：
     | | 動画なし（v1.41） | 動画あり（v1.42） |
     |---|---|---|
     | スタートを押してからホームが出るまで | 4.3秒 | 4.1秒 |
     | ホームで10秒の平均コマ数 | 55.5 | 55.4 |
     | 50ms を超えたコマ（10秒） | 4 | 6 |
     | 動画が流れ始めるまで | ― | ほぼすぐ（10秒後に9.7秒の位置） |
     動画のデータは初回に約3.5MB。2回目からはブラウザの控えを使う。

161. **ホームを「動画・最近のカード3枚・称号」にした（v1.43）。**
     - 動画の下に、最近手に入れたカードを3枚ならべる。はじめは新しい順。10秒ごとに、前と違うカードを優先して3枚えらび直す
       （新しいカードほど出やすい）。押すとカードの詳細へ。3枚以下なら入れ替えない。足りない枠はカードの裏（押すとガチャへ）
     - 初めての人はカードが無いので、3枚の代わりに「10連ガチャ」のポップ
     - その下に称号5つを横一列に。画面の高さに収め、スクロールさせない（初めての人は動画の高さを抑える）

162. **称号を押すと、獲得条件・あといくつで獲得か・ガチャ／マップへの案内・SNSでシェアを出すようにした（v1.43）。**
     ホームとミッション画面の両方（js/titles.js）。
     - 獲得条件（例：グルメのカードを、すべて（32種類）集める）と、進み具合のバー
     - 「あと ○ 種類で称号獲得！」（志賀町コンプリートは「あと カード ○ 種類・チェックイン ○ か所で称号獲得！」）
     - 未獲得なら「ガチャを引いてカードを集める」、スポット・志賀町マスター・志賀町コンプリートは「マップでスポットを探す」も
     - SNSでシェア：獲得済みは称号の絵とURL、未獲得は「称号を目指しています！ あと○種類」の文とURL

163. **まち巡り画面の一覧をやめ、地図の絞り込みにした（v1.43）。**
     未訪問・訪問済みの一覧と「石川県志賀町」を外し、その場所に横にスクロールできるボタンを置いた。
     すべて／未訪問（チェックインがまだ）／訪問済み（チェックインした）／
     歴史と絶景を巡るモデルコース（045 041 036 035 042 046）／絶景スポットを巡るモデルコース（043 041 038 037 036 035 033 034）。
     選んだものに当てはまるスポットのピンだけを出す。ピンは今までどおり、訪問済みは金の星、まだの所はピン。
     「すべて」以外は、選んだピンが全部入るように地図の真ん中と拡大を合わせる。当てはまるスポットが無いときは地図の真ん中に知らせを出す。

164. **ホームとミッション画面を整えた（v1.44）。**
     - ミッション画面の称号の欄と、初回の案内の称号の説明を外した（称号はホームにある）
     - 称号「志賀町コンプリート」の獲得演出は、ホームの称号の枠へ飛んでいってはまるようにした
     - ホームは余白を必要な分だけにして上から詰める。動画は 16:9（初めての人は 4:3）を目安に、
       画面に余りがあれば 4:3 まで伸ばし、足りなければ縮める（カード3枚と称号の大きさは変えない）
     - カード3枚の上に「最近手に入れたカード」と「53種類中 ○種類あつめた」。初めての人は「カードを集めよう」
     - 称号は1段に3つずつ2段（3つ・2つ）。丸い絵の右に名前を2行で置いて、高さを抑える

165. **まち巡り画面の絞り込みを3段にした（v1.44）。**
     1段目：すべて・未訪問・訪問済み／2段目：歴史と絶景を巡る志賀町観光モデルコース／3段目：絶景スポットを巡る志賀町観光モデルコース。
     横にスクロールさせず、文字ははみ出さない範囲で大きく（画面幅の4%、12.5〜16px）。
     選んだボタンは黒ではなく、「近くのスポットを探す」と同じ緑。

166. **初回の10連を引いたあと、ガチャ画面に戻ってきたときに「ミッションをクリアしてSHIKA COINをもらおう！」の案内を出す（v1.44）。**
     初回の10連を引く前（はじめましての画面）では出さない。
     ガチャは SHIKA COIN で引けること、コインはミッション・ログインボーナス・チェックインでもらえることを伝え、
     「ミッションを見る」ボタンでミッション画面へ案内する。ほかの場所をタップすると閉じる。1回だけ（flags.gachaGuideShown）。

167. **称号とミッションに「志賀町ファンクラブ」を加えた（v1.45）。**
     LINE の志賀町ファンクラブのページ（https://liff.line.me/2006352925-134gxgYw/landing?follow=%40299bzzpg&lp=VODReE&liff_id=2006352925-134gxgYw）を開き、
     受信設定フォームから志賀町ファンクラブに登録してもらう。登録したかはアプリから確かめられない（LINE と連携していない）ので、
     「ファンクラブに登録」ボタンを押したことで登録とみなす（flags.fanclubJoined。js/fanclub.js）。
     - 称号「志賀町ファンクラブ」：志賀町マスターの左（ホームでは2段目のいちばん左）。絵は assets/icons/title-fanclub.png（512）と -sm（128）
       押すと、獲得条件と「ファンクラブに登録（LINE が開きます）」ボタン。獲得済みならシェア
     - ミッション：カードの上に「ファンクラブ」を置き、「志賀町ファンクラブ会員になる」（+10 SHIKA COIN、config.json の mission.fanclub）
       その下に「ファンクラブに登録」ボタンをまとめる。ミッションを押すと、受信設定フォームから登録する方法を説明する
     - 管理モードに「ファンクラブ登録を取り消す」（確認用）
     アプリから LINE へ送る情報は無い（リンクを開くだけ）。

168. **まち巡り画面の絞り込みの文字と余白を小さくした（v1.45）。**
     文字が大きすぎて画面がスクロールしていた。文字は画面幅の3.4%（11.5〜13.5px）、上下の余白は6px。
     地図の高さも画面に合わせる（上のおび・タブ・絞り込み・ボタンなどを引いた高さ。220〜420px）。小さい画面でもスクロールしない。

169. **すべての称号で、獲得の演出を出す（v1.46）。**
     これまで「志賀町コンプリート」だけだった演出を、グルメ／スポット／文化マスター・志賀町ファンクラブ・志賀町マスターにも出す（js/title-complete.js）。
     ホームへ移って画面を暗くし、称号を大きく出して回して表にし、ホームのその称号の枠へ飛んでパチンとはまる。
     - 称号ごとに1回だけ。見せた称号は state.titlesCelebrated に残す（コンプリートは flags.completeCelebrated も）
     - 同時に複数獲得したら、1つずつ順に出す。演出を見るまで、ホームの枠は未獲得の見た目のまま
     - v1.45 までに持っていた称号は、はじめて確かめたときに見せたことにする（更新してすぐ演出が並ばないように）
     - ガチャの演出中・結果の確認中・お知らせ・案内・3Dで見る画面・LINE を開いて離れているあいだは待つ
     - 管理モード：称号を選んで「称号の演出を見る」（状態は変えない）、「獲得済みの称号の演出をもう一度出す」

170. **称号がはまる動きのカクつきを直した（v1.46）。**
     - 枠のところだけ照らす幕を、グラデーションの描き直しではなく、丸い部品の透明度の入れ替えにした
     - 枠へ飛ぶ動きは、その時点の見た目から Web Animations で動かす（途中で大きさが跳ばない）。飛ぶあいだは後ろの光・粒・影を止める
     - はまった瞬間の光の輪は box-shadow を動かさず、疑似要素の大きさと透明度だけで広げる
     - 保存の書き込みは「パチーン」の動きが終わってから。枠の絵は作り直さない
     ヘッドレス Edge（390×844）で、飛び始めからはまるまでのコマの間隔が最大17ms（34ms超えなし）。

171. **称号を押したときの案内の絵を大きくした（v1.46）。** 枠 104px → 172px（画面幅の46%まで）、絵は枠の88%。

172. **起動画面で「スタート」（「スキップ」）を押すと、必ずホームへ移る（v1.46）。** どの画面のアドレスで開いても、ホームから始める。

173. **ホームの動画を作り直した（v1.47）。**
     - 「志賀町を、あつめよう」の文字が出そろったところ（37.0秒）を3秒のばしたが、
       動きが止まって見えるため v1.47.1 で元に戻した（37秒のまま。ゆっくり寄る止め絵もやめた）
     - 画質を上げた：960×408・約0.75Mbps（3.5MB）→ 1152×486・約1.2Mbps（5.5MB）。
       画面の細かさが3倍の端末（幅390px）でも足りる大きさ。最初の場面の絵（home-poster.jpg）も作り直した
     - ヘッドレス Edge（390×844）で、流しているあいだのコマの間隔は最大18.6ms、取りこぼしたコマ0

174. **ホームの動画を、タブを移っても続きから流す（v1.47）。**
     ほかのタブへ移るときに、どこまで流したかを覚えておき（homeVideoTime）、戻ってきたらその続きから。
     終わりぎわ（溶けはじめる前0.2秒より後）だったときと、最初に戻ったあとは、最初から。
     読み込みを手放すのは今までどおり（ホームを離れたら止めて手放す）。

175. **起動画面のうしろで、前に開いていたタブを組み立てない（v1.47）。**
     地図やカードのアドレスのまま読み込み直して「スタート」を押すと、その画面が一瞬見えていた。
     起動画面を出す前にホームへ置き換える（history.replaceState）ので、組み立てるのはホームだけになる。

176. **称号の大きい絵を軽い WebP にして、先に読んでおく（v1.47）。**
     「スタート」の直後に称号を押すと、1枚1.4MBほどの PNG を読んで展開するため、絵が出るまでカクついていた。
     - assets/frames/web/icon-*.webp・assets/icons/web/title-*.webp（720px・1枚およそ100KB）を用意し、先に試す（読めなければ元の PNG）
     - 起動画面が消えたあと、手が空いたときに1枚ずつ先に読む（app.js の prefetchTitleArt）
     ヘッドレス Edge で、押してから絵が出るまで59ms・34msを超えるコマなし。

177. **まち巡り画面に、選んだ絞り込みのスポット一覧を出した（v1.48）。**
     「近くのスポットを探す」の下に、いま選んでいる絞り込みのスポットを並べる（js/map.js の drawList）。
     - 行には、番号（モデルコースは巡る順の1・2・3…、ほかは訪問済みの★か●）、名前、状態、
       現在地が分かっていれば直線のおよその距離。押すとそのカードの詳細へ
     - 行ごとに「経路」ボタン（Googleマップの経路検索。行き先だけを渡す）
     - モデルコースには「コース全体の経路をGoogleマップで見る」（v1.48.1 で現在地から回る形に直した。178）
     コースの順番は、歴史と絶景＝No.45・41・36・35・42・46、絶景スポット＝No.43・41・38・37・36・35・33・34。
     現在地は Googleマップにも渡さない（アプリは端末の位置を保存も送信もしない）。

178. **コース全体の経路を、現在地から道のりが短くなる順にした（v1.48.1）。**
     v1.48 は1番目のスポットを出発地にしていたので、いまいる場所から遠い所へ先に向かう経路になっていた。
     - 出発地は渡さず、現在地から出発する経路にする（Googleマップ側が現在地を使う。アプリからは位置を渡さない）
     - 回る順番は、アプリの中で計算して並べ替える（js/map.js の bestOrder）。
       8か所までは、ありうる順番をすべて試して最短を選ぶ（途中で長くなったら打ち切る）。それより多いときは近い所から順に。
       距離は直線のめやす。8か所で約5ms
     - 押した瞬間の現在地で計算し直すので、移動していても近い順になる
     - 一覧には「経路の順 n番目」を添え、注意書きに経路の順（例：3 → 4 → 2 → 5 → 1 → 6）を出す
     - 現在地がまだ分からないときは「現在地を取得して、近い順にする」ボタンを出す（チェックインはしない）

179. **「近くのスポットを探す」まわりを整理した（v1.48.2）。**
     - 現在地を探しているあいだの文言を「現在地を確認しています…」だけにした（「ピン！ 波紋」「志賀町を探索中…」の入れ替えをやめた。js/geo.js）
     - 現在地が分かったら、どの絞り込みでも一覧を現在地から近い順に並べる（js/map.js の drawList）。
       モデルコースの番号は、コースで決めた順の番号のまま添える
     - どのスポットの範囲にも入っていないときの「チェックイン範囲外です」の案内をやめた。
       一覧が近い順に並び替わり、それぞれの距離も出るので、案内を出さずに済む

180. **ミッション「志賀町と日本酒の歴史」を追加した（v1.49）。**
     ミッション画面の「ファンクラブ」の下に置く。内容は「志賀町と日本酒の歴史を読む」、報酬は5 SHIKA COIN
     （config.json の mission.sake で変えられる。ファンクラブの10も mission.fanclub に書き出した）。
     - ミッションの下の「読む」ボタン（または行をタップ）で本文が出て、その時点で達成（flags.sakeHistoryRead）。
       行の右は、ほかのミッションと同じくコインの絵と「+5」（v1.49.1。行にもボタンを置くと「読む」が2つ並ぶため）
     - 本文は町からの提供文をそのまま載せる（js/missions.js の SAKE_HISTORY）。
       四戸の酒蔵（岡部弥平・加茂野八郎・辻口政頼・泉庄助）と、『日本の食生活全集17 聞き書 石川の食事』の冬の食文化
     - 受け取りは今までどおり「受け取る」を押す。読み直しても二重には受け取れない
     - ミッションの下に「読む／もう一度読む」ボタンのまとまり（ファンクラブと同じ形）

## 今後やるとよいこと

- 詳細画面用の写真（`詳細写真2` `詳細写真3` 列）
- 説明文・旬・座標の内容確認（特に能登半島地震以降の営業・通行状況）
